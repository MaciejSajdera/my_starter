import type { RequestHandler } from "msw";

/**
 * Add your custom handlers here. (Or import/spread example handlers.)
 *
 * IMPORTANT: This array should be EMPTY when merging, so that only empty array is bundled in production.
 */
export const mswHandlers: RequestHandler[] = [];
